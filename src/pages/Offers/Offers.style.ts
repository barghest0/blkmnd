import styled from 'styled-components';
import { container, page } from '../../shared/styles/mixins';

const Offers = styled.div`
  ${page}
`;

const Container = styled.div`
  ${container}
`;

export { Offers, Container };
