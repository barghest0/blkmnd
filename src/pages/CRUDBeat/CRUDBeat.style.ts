import styled from 'styled-components';
import { container, page } from '../../shared/styles/mixins';

const CRUDBeat = styled.div`
  ${page}
`;

const Container = styled.div`
  ${container}
`;

const Title = styled.div``;

export { CRUDBeat, Container, Title };
