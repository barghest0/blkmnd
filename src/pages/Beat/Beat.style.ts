import styled from 'styled-components';
import { container } from '../../shared/styles/mixins';

const Beat = styled.div``;

const Container = styled.div`
  ${container}
`;

export { Beat, Container };
