import styled from 'styled-components';

import { styled as MUIstyled } from '@mui/material';

const CollabForm = styled.form`
  display: grid;
  grid-template-columns: 1fr;
  grid-auto-rows: 50px;
  row-gap: 20px;
`;

const Submit = styled.div``;

export { CollabForm, Submit };
