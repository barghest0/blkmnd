import styled from 'styled-components';

const MembershipList = styled.div``;

const MembershipCard = styled.div``;

const Title = styled.p`
  text-transform: uppercase;
  font-weight: normal;
`;

export { MembershipList, Title, MembershipCard };
